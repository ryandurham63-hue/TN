name: CI
on: Meta
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ] 
  workflow_dispatch: H
jobs:  svc.sh
  build: env.sh
runs-on: Ubunta-latest 
    steps: steps.build-image.outputs.image 
      - uses: actions/checkout@v4
      - name: Run a one-line script
        run: echo Hello, Boar's Head
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
